# Game Backend

Spring Boot REST API for the game application.

## Running the application

```bash
mvn spring-boot:run
```

The server will start on http://localhost:8080

## API Endpoints

- GET /api/maps - Get all maps
- GET /api/maps/{id} - Get map by ID

## Data Folder

Map data is stored in the `data/maps` folder.
